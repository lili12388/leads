{
  "name": "whatsapp-sender",
  "version": "1.0.0",
  "description": "WhatsApp automation tool for sending messages to leads",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . WhatsApp-Sender --platform=win32 --arch=x64 --out=dist --overwrite --asar"
  },
  "dependencies": {
    "puppeteer": "^21.0.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2"
  }
}
